import { Card, CardHeader, CardTitle } from "@/components/ui/card"

export const Login = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>
          <h1 className="text-xl text-black">Bem-vindo de volta</h1>
        </CardTitle>
      </CardHeader>
    </Card>
  )
}
